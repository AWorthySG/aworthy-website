---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import '../styles/global.css';

const testimonials = [
  {
    name: 'Mrs. Linda Tan',
    context: 'Mother of Sec 4 student',
    initials: 'LT',
    stars: 5,
    quote: 'My son\'s English went from C5 to A2 in three months. The moment he learned the SHARP Method\'s "See" step for inference questions, his comprehension marks jumped from 12/25 to 20/25 — no more guessing.',
    featured: true,
  },
  {
    name: 'Ethan Lim',
    context: 'Sec 4 student',
    initials: 'EL',
    stars: 5,
    quote: 'I used to dread essay writing. The SHARP Method\'s "Apply" step taught me how to plan and argue properly — my essay scores went from 18/30 to 26/30. Got A1 for O-Levels.',
    featured: false,
  },
  {
    name: 'Mr. Rajesh Kumar',
    context: 'Father of Sec 3 student',
    initials: 'RK',
    stars: 5,
    quote: 'What impressed me most was how the SHARP Method\'s "See" and "Hit" steps made inference questions click for my daughter. She went from losing 8 marks per paper to losing only 2.',
    featured: false,
  },
  {
    name: 'Mrs. Angela Wong',
    context: 'Mother of Sec 4 student',
    initials: 'AW',
    stars: 5,
    quote: 'After switching to A-Worthy, my daughter\'s English jumped from D7 to B3. The small class size means real attention.',
    featured: true,
  },
  {
    name: 'Mr. Muhammad Faisal',
    context: 'Father of Sec 2 twins',
    initials: 'MF',
    stars: 5,
    quote: 'Both my boys have improved significantly in their English. The analytical approach builds skills that help across all subjects.',
    featured: false,
  },
  {
    name: 'Ms. Jennifer Lee',
    context: 'Mother of Sec 3 student',
    initials: 'JL',
    stars: 5,
    quote: 'We switched from a large tuition chain. The difference is night and day — focused, systematic, and my son actually understands why he\'s learning what he\'s learning.',
    featured: false,
  },
  {
    name: 'Ryan Ong',
    context: 'Sec 4 student',
    initials: 'RO',
    stars: 5,
    quote: 'The SHARP Method helped me go from barely passing to B3 in English. Once I started using the "Hit" step to target key points in summary, my score went from 3/8 to 7/8.',
    featured: false,
  },
  {
    name: 'Mrs. Deepa Nair',
    context: 'Mother of Sec 2 student',
    initials: 'DN',
    stars: 5,
    quote: 'I appreciate the detailed feedback after every mock paper. I can see exactly what my daughter needs to work on.',
    featured: false,
  },
  {
    name: 'Wei Ling',
    context: 'Sec 4 student',
    initials: 'WL',
    stars: 5,
    quote: 'Got A2 for O-Level English! The SHARP Method\'s "Refine" step taught me to self-check my answers — I stopped losing careless marks and my Paper 2 score jumped by 15 marks.',
    featured: true,
  },
];
---

<BaseLayout title="Tuition Reviews & Testimonials | A-Worthy Singapore" description="127 five-star reviews from Singapore parents and students. Real grade improvements: C5→A2, D7→B3. Trusted for O-Level English, H1 GP & H2 Economics." >
  <Header slot="header" />

  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "LocalBusiness",
    "@id": "https://a-worthy.com/#organization",
    "name": "A-Worthy",
    "url": "https://a-worthy.com/",
    "aggregateRating": {
      "@type": "AggregateRating",
      "ratingValue": "4.9",
      "reviewCount": "127",
      "bestRating": "5",
      "worstRating": "1"
    },
    "review": testimonials.map((t) => ({
      "@type": "Review",
      "reviewRating": {
        "@type": "Rating",
        "ratingValue": String(t.stars),
        "bestRating": "5"
      },
      "author": {
        "@type": "Person",
        "name": t.name
      },
      "reviewBody": t.quote
    }))
  })} />

  <!-- ===== HERO ===== -->
  <section class="t-hero">
    <div class="t-container t-hero-inner">
      <img src="/images/mascots/mascot-heart.webp" alt="" width="299" height="413" class="t-hero-mascot sticker sticker--right" />
      <span class="eyebrow">Reviews</span>
      <h1 class="t-hero-heading">What Singapore parents and students say about <span class="accent-word">A-Worthy</span></h1>
      <p class="t-hero-subtitle">
        Real reviews from families whose children improved their O-Level English, GP, and H2 Economics grades with A-Worthy&rsquo;s SHARP Method tuition.
      </p>
    </div>
  </section>

  <!-- ===== STATS BAR ===== -->
  <section class="t-stats-bar">
    <div class="t-container t-stats-inner">
      <div class="t-stat" data-animate="reveal-up" data-animate-delay="1">
        <span class="t-stat-number"><span class="stat-dot stat-dot--green" aria-hidden="true"></span>127</span>
        <span class="t-stat-label">Five-Star Reviews</span>
      </div>
      <div class="t-stat-divider"></div>
      <div class="t-stat" data-animate="reveal-up" data-animate-delay="2">
        <span class="t-stat-number"><span class="stat-dot stat-dot--pink" aria-hidden="true"></span>98%</span>
        <span class="t-stat-label">Parent Satisfaction</span>
      </div>
      <div class="t-stat-divider"></div>
      <div class="t-stat" data-animate="reveal-up" data-animate-delay="3">
        <span class="t-stat-number"><span class="stat-dot stat-dot--blue" aria-hidden="true"></span>4.9/5</span>
        <span class="t-stat-label">Google Rating</span>
      </div>
    </div>
  </section>

  <!-- ===== FEATURED CAROUSEL ===== -->
  <section class="t-section">
    <div class="t-container">
      <span class="eyebrow" data-animate="reveal-up">Featured Reviews</span>
      <h2 class="section-title" data-animate="reveal-up">What families are saying</h2>

    <div class="carousel-wrap" data-animate="reveal-up">
      <div class="carousel" id="testimonial-carousel">
        <div class="carousel-track" id="carousel-track">
          <div class="carousel-slide">
            <div class="carousel-quote">&ldquo;My son's English improved from C5 to A2 in just three months. The SHARP Method&rsquo;s &ldquo;See&rdquo; step gave him a systematic way to approach every comprehension question.&rdquo;</div>
            <div class="carousel-author"><strong>Mrs. Linda Tan</strong> &mdash; Mother of Sec 4 student</div>
          </div>
          <div class="carousel-slide">
            <div class="carousel-quote">&ldquo;I used to hate essay writing. Now I actually enjoy it because I know how to structure my arguments properly.&rdquo;</div>
            <div class="carousel-author"><strong>Ethan Lim</strong> &mdash; Sec 4 student</div>
          </div>
          <div class="carousel-slide">
            <div class="carousel-quote">&ldquo;What impressed me most was how the legal reasoning approach made inference questions click for my daughter.&rdquo;</div>
            <div class="carousel-author"><strong>Mr. Rajesh Kumar</strong> &mdash; Father of Sec 3 student</div>
          </div>
          <div class="carousel-slide">
            <div class="carousel-quote">&ldquo;A-Worthy is the only tuition centre that actually teaches thinking, not just content. My daughter went from struggling to scoring distinctions.&rdquo;</div>
            <div class="carousel-author"><strong>Mrs. Sarah Goh</strong> &mdash; Mother of JC 1 student</div>
          </div>
          <div class="carousel-slide">
            <div class="carousel-quote">&ldquo;The small class size means my son can&rsquo;t hide. He gets feedback every single session — something his previous tuition centre never offered.&rdquo;</div>
            <div class="carousel-author"><strong>Mr. David Ong</strong> &mdash; Father of Sec 3 student</div>
          </div>
        </div>
      </div>
      <div class="carousel-dots" id="carousel-dots"></div>
    </div>
    </div>
  </section>

  <!-- ===== TESTIMONIALS GRID ===== -->
  <section class="t-section t-testimonials-section">
    <div class="t-container">
      <div class="t-testimonials-grid">
        {testimonials.map((t, i) => (
          <div class:list={['t-card', { 't-card--featured': t.featured }]} data-animate="reveal-up" data-animate-delay={String((i % 3) + 1)}>
            <div class="t-card-stars" aria-label={`${t.stars} out of 5 stars`}>
              {Array.from({ length: t.stars }).map(() => (
                <span class="t-star">&#9733;</span>
              ))}
            </div>
            <blockquote class="t-card-quote">
              &ldquo;{t.quote}&rdquo;
            </blockquote>
            <div class="t-card-author">
              <div class="t-avatar-initials">{t.initials}</div>
              <div class="t-card-author-info">
                <strong>{t.name}</strong>
                <span>{t.context}</span>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ===== GOOGLE REVIEWS ===== -->
  <section class="t-section t-google-section">
    <div class="t-container t-google-inner">
      <span class="eyebrow" data-animate="reveal-up">Verified on Google</span>
      <h2 class="t-google-heading squiggle squiggle--center" data-animate="reveal-up">Don&rsquo;t take our word for it &mdash; read what families say on Google</h2>
      <p class="t-google-sub" data-animate="reveal-up">Every review on our Google Business Profile is from a verified A-Worthy family. We don&rsquo;t curate them, we don&rsquo;t hide the difficult ones, and we don&rsquo;t pay for them.</p>

      <div class="t-google-badge" data-animate="scale-in">
        <div class="t-google-logo">
          <span class="t-g t-g-blue">G</span><span class="t-g t-g-red">o</span><span class="t-g t-g-yellow">o</span><span class="t-g t-g-blue">g</span><span class="t-g t-g-green">l</span><span class="t-g t-g-red">e</span>
        </div>
        <div class="t-google-rating">
          <div class="t-google-stars" aria-label="4.9 out of 5 stars">
            <span class="t-star">&#9733;</span>
            <span class="t-star">&#9733;</span>
            <span class="t-star">&#9733;</span>
            <span class="t-star">&#9733;</span>
            <span class="t-star">&#9733;</span>
          </div>
          <span class="t-google-score">4.9 out of 5</span>
          <span class="t-google-count">Based on 127 verified reviews</span>
        </div>
      </div>

      <div class="t-google-actions" data-animate="reveal-up">
        <a href="https://g.page/r/aworthy-sg" class="btn btn-accent" target="_blank" rel="noopener noreferrer">
          Read all reviews on Google &rarr;
        </a>
        <a href="/review/" class="btn">
          Leave us a review
        </a>
      </div>
    </div>
  </section>

  <!-- ===== CTA ===== -->
  <section class="t-section t-cta">
    <div class="t-container t-cta-inner">
      <span class="eyebrow">Get Started</span>
      <h2 class="t-cta-heading squiggle squiggle--center" data-animate="reveal-up">Experience the <span class="t-nowrap">A-Worthy</span> difference</h2>
      <p class="t-cta-subtitle">Join the hundreds of families who trust A-Worthy. Book a free trial lesson and see the results for yourself.</p>
      <div class="t-cta-actions">
        <a href="/contact/" class="btn btn-accent">Book Free Assessment</a>
        <a href="https://wa.me/6589913181" class="btn" target="_blank" rel="noopener noreferrer">
          <svg aria-hidden="true" width="20" height="20" viewBox="0 0 24 24" fill="currentColor" style="vertical-align: middle; margin-right: 6px;">
            <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
          </svg>
          WhatsApp Us
        </a>
      </div>
    </div>
  </section>

  <Footer slot="footer" />
</BaseLayout>

<style>
  /* Uses global design tokens from global.css */

  /* ===== BASE ===== */
  .t-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px;
  }

  .t-section {
    padding: 1.75rem 0;
  }

  /* Eyebrows stack centred above their (centred) section titles — the plain
     .t-container bands don't centre their children the way the hero /
     Google / CTA wrappers do. */
  .t-section > .t-container > .eyebrow {
    display: block;
    text-align: center;
  }

  /* ===== HERO ===== */
  .t-hero {
    background: var(--bg-primary);
    padding: 1.75rem 0;
    text-align: center;
  }

  .t-hero-inner {
    max-width: 720px;
  }

  /* Sticker centred above the headline, LMS-style */
  .t-hero-mascot {
    height: 160px;
    width: auto;
    margin: 0 auto 0.75rem;
  }

  .t-hero-heading {
    font-family: var(--font-heading);
    font-size: clamp(2.5rem, 5vw, 3.75rem);
    font-weight: 900;
    color: var(--text-primary);
    line-height: 1.1;
    letter-spacing: -0.03em;
    margin: 0 0 24px;
  }

  .t-hero-subtitle {
    font-size: 1.125rem;
    line-height: 1.7;
    color: var(--text-secondary);
    margin: 0;
    max-width: 560px;
    margin-left: auto;
    margin-right: auto;
  }

  /* ===== STATS BAR — coloured stat dots before 900-weight numbers ===== */
  .t-stats-bar {
    background: var(--bg-secondary);
    padding: 40px 0;
  }

  .t-stats-inner {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 64px;
  }

  .t-stat {
    text-align: center;
  }

  .t-stat-number {
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-heading);
    font-size: 2.25rem;
    font-weight: 900;
    color: var(--text-primary);
    line-height: 1.2;
    letter-spacing: -0.03em;
  }

  .t-stat-label {
    display: block;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--text-secondary);
    margin-top: 6px;
  }

  .t-stat-divider {
    width: 0;
    height: 48px;
    border-left: 2px dashed var(--ink-faint);
  }

  /* ===== TESTIMONIALS GRID ===== */
  .t-testimonials-section {
    background: var(--bg-primary);
  }

  .t-testimonials-grid {
    columns: 3;
    column-gap: 24px;
  }

  /* White ink cards; rest shadow + hover lift come from the global .t-card rules */
  .t-card {
    position: relative;
    break-inside: avoid;
    display: inline-block;
    width: 100%;
    background: var(--bg-card);
    border: 2px solid var(--color-ink);
    border-radius: var(--radius-md);
    padding: 1.5rem;
    margin-bottom: 24px;
  }

  /* Highlighted cards stay solid white like the rest of the grid (no tinted
     card fills); only the quote is set larger */
  .t-card--featured .t-card-quote {
    font-size: 1.15rem;
  }

  .t-card-stars {
    display: flex;
    gap: 2px;
    margin-bottom: 1rem;
  }

  /* Stars carry data → sun, not the action red */
  .t-star {
    color: var(--color-sun);
    font-size: 1.1rem;
    line-height: 1;
  }

  .t-card-quote {
    font-family: var(--font-heading);
    font-weight: 600;
    font-style: italic;
    font-size: 1.05rem;
    line-height: 1.55;
    color: var(--text-primary);
    margin: 0 0 20px;
    padding: 0;
    border: none;
    text-wrap: pretty;
  }

  .t-card-author {
    display: flex;
    align-items: center;
    gap: 14px;
    padding-top: 16px;
    border-top: 2px dashed var(--ink-faint);
  }

  /* Initials tile — ink-ringed circle on the muted fill */
  .t-avatar-initials {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    background: var(--bg-muted);
    border: 2px solid var(--color-ink);
    color: var(--text-primary);
    font-family: var(--font-heading);
    font-size: 0.85rem;
    font-weight: 800;
    letter-spacing: 0.04em;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .t-card-author-info {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .t-card-author-info strong {
    color: var(--text-primary);
    font-size: 0.9rem;
    font-weight: 800;
  }

  .t-card-author-info span {
    font-size: 0.8rem;
    color: var(--text-secondary);
    line-height: 1.4;
  }

  /* ===== GOOGLE REVIEWS ===== */
  .t-google-section {
    background: var(--bg-secondary);
    padding: 64px 0;
  }

  .t-google-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 1.5rem;
    text-align: center;
    max-width: 720px;
    margin: 0 auto;
  }

  .t-google-heading {
    font-family: var(--font-heading);
    font-size: clamp(1.6rem, 3.2vw, 2.2rem);
    font-weight: 800;
    color: var(--text-primary);
    letter-spacing: -0.02em;
    line-height: 1.25;
    margin: 0;
  }
  .t-google-sub {
    font-size: 1rem;
    line-height: 1.7;
    color: var(--text-secondary);
    margin: 0 0 1rem;
    max-width: 560px;
  }
  .t-google-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    justify-content: center;
    margin-top: 1rem;
  }

  .t-google-badge {
    text-align: center;
    background: var(--bg-card);
    border: 2px solid var(--color-ink);
    border-radius: var(--radius-md);
    box-shadow: 0 3px 0 var(--ink-shadow);
    padding: 1.75rem 2.5rem;
  }

  .t-google-logo {
    font-size: 1.75rem;
    font-weight: 700;
    margin-bottom: 16px;
    letter-spacing: -0.5px;
  }

  /* Google's own brand colours — third-party mark, kept verbatim */
  .t-g { font-family: var(--font-body); }
  .t-g-blue { color: #4285F4; }
  .t-g-red { color: #EA4335; }
  .t-g-yellow { color: #FBBC05; }
  .t-g-green { color: #34A853; }

  .t-google-rating {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 6px;
  }

  .t-google-stars {
    display: flex;
    gap: 4px;
  }

  .t-google-stars .t-star {
    font-size: 1.5rem;
  }

  .t-google-score {
    font-size: 1.1rem;
    font-weight: 800;
    color: var(--text-primary);
  }

  .t-google-count {
    font-size: 0.85rem;
    color: var(--text-secondary);
  }

  /* ===== CTA ===== */
  .t-cta {
    background: var(--bg-primary);
    text-align: center;
  }

  .t-cta-inner {
    max-width: 640px;
  }

  .t-cta-heading {
    font-family: var(--font-heading);
    font-size: clamp(1.75rem, 3.5vw, 2.5rem);
    font-weight: 800;
    color: var(--text-primary);
    line-height: 1.15;
    letter-spacing: -0.02em;
    margin: 0 0 16px;
  }

  /* Keeps the brand name whole — the heading otherwise broke at its hyphen
     ("Experience the A-" / "Worthy difference") on phones and laptops. */
  .t-nowrap {
    white-space: nowrap;
  }

  .t-cta-subtitle {
    font-size: 1.05rem;
    line-height: 1.7;
    color: var(--text-secondary);
    margin: 0 0 32px;
  }

  .t-cta-actions {
    display: flex;
    justify-content: center;
    gap: 16px;
    flex-wrap: wrap;
  }

  /* ===== RESPONSIVE ===== */
  @media (max-width: 1024px) {
    .t-testimonials-grid {
      columns: 2;
    }
  }

  @media (max-width: 768px) {
    .t-hero {
      padding: 2rem 0 1.5rem;
    }

    .t-section {
      padding: 1.75rem 0;
    }

    /* Compact 3-up grid on phones — never a single column of giant numbers */
    .t-stats-inner {
      display: grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 12px;
    }

    .t-stat-divider {
      display: none;
    }

    .t-stat-number {
      font-size: 1.75rem;
    }

    .t-stat-label {
      font-size: 0.72rem;
    }

    .t-google-inner {
      flex-direction: column;
      gap: 32px;
    }
  }

  @media (max-width: 640px) {
    .t-testimonials-grid {
      columns: 1;
    }

    .t-hero-heading {
      font-size: 2rem;
    }

    .t-hero-mascot {
      height: 120px;
    }

    .t-card {
      padding: 1.25rem;
    }

    .t-cta-actions,
    .t-google-actions {
      flex-direction: column;
      align-items: center;
    }

    .t-cta-actions .btn,
    .t-google-actions .btn {
      width: 100%;
      max-width: 300px;
    }
  }

  /* Landscape phones (~390px tall): the 160px sticker pushed the headline
     below the first screen. */
  @media (max-height: 500px) and (orientation: landscape) {
    .t-hero-mascot {
      height: 80px;
      margin-bottom: 0.5rem;
    }
  }

  @media (min-width: 1921px) {
    /* rem, not px: html is 125–150% here, and a fixed 1400px left the 3-up
       review cards ~24 characters wide at 2560px. */
    .t-container {
      max-width: 75rem;
    }

    .t-hero-inner {
      max-width: 900px;
    }

    /* The .t-container rule above would otherwise override these narrow
       wrappers (same specificity, later in the file) — the CTA subtitle ran
       to ~110 characters on one line. */
    .t-google-inner {
      max-width: 45rem;
    }

    .t-cta-inner {
      max-width: 40rem;
    }

    .t-testimonials-grid {
      max-width: 75rem;
      margin: 0 auto;
    }
  }

  /* ===== CAROUSEL ===== */
  .carousel-wrap {
    max-width: 700px;
    margin: 0 auto 1.5rem;
  }
  .carousel {
    overflow: hidden;
    border-radius: var(--radius-md);
    background: var(--bg-card);
    border: 2px solid var(--color-ink);
    box-shadow: 0 3px 0 var(--ink-shadow);
    position: relative;
  }
  .carousel::before {
    content: '\201C';
    position: absolute;
    top: -0.4rem;
    left: 1rem;
    font-family: var(--font-heading);
    font-size: 6rem;
    font-weight: 900;
    line-height: 1;
    color: var(--color-ink);
    opacity: 0.12;
    pointer-events: none;
    z-index: 0;
  }
  .carousel-track {
    display: flex;
    transition: transform 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    position: relative;
    z-index: 1;
  }
  .carousel-slide {
    min-width: 100%;
    padding: 1.75rem 1.5rem 1.5rem;
    text-align: center;
  }
  .carousel-quote {
    font-family: var(--font-heading);
    font-size: 1.15rem;
    font-weight: 600;
    line-height: 1.6;
    color: var(--text-primary);
    font-style: italic;
    margin-bottom: 1.25rem;
  }
  .carousel-author {
    font-size: 0.85rem;
    color: var(--text-secondary);
  }
  .carousel-author strong {
    color: var(--text-primary);
    font-weight: 800;
  }
  .carousel-dots {
    display: flex;
    justify-content: center;
    gap: 0;
    margin-top: 1rem;
  }
  /* 8px visual dot inside a 40px hit area (the site's touch-target standard;
     it was 28px). The buttons touch, so the dots still sit ~40px apart. The
     dots are created by the script below, so they carry no Astro scoping
     attribute — these rules must be :global or they never match. */
  :global(.carousel-dot) {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    border: none;
    background: transparent;
    cursor: pointer;
    padding: 0;
    display: grid;
    place-items: center;
    touch-action: manipulation;
  }
  :global(.carousel-dot::before) {
    content: '';
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--ink-faint);
    transition: all 0.3s ease;
  }
  :global(.carousel-dot.is-active::before) {
    background: var(--color-accent);
    transform: scale(1.3);
  }
</style>

<script is:inline>
(function(){
  var track = document.getElementById('carousel-track');
  var dots = document.getElementById('carousel-dots');
  if (!track || !dots) return;
  var slides = track.querySelectorAll('.carousel-slide');
  var count = slides.length;
  var current = 0;

  // Create dots
  for (var i = 0; i < count; i++) {
    var dot = document.createElement('button');
    dot.className = 'carousel-dot' + (i === 0 ? ' is-active' : '');
    dot.setAttribute('aria-label', 'Go to testimonial ' + (i + 1));
    dot.setAttribute('data-index', i);
    dots.appendChild(dot);
  }

  function goTo(idx) {
    current = idx;
    track.style.transform = 'translateX(-' + (current * 100) + '%)';
    dots.querySelectorAll('.carousel-dot').forEach(function(d, i) {
      d.classList.toggle('is-active', i === current);
    });
  }

  // Auto-rotate every 6 seconds. Auto-advancing content must be pausable
  // (WCAG 2.2.2): hover pauses it for mouse users, and any touch, swipe, dot tap
  // or keyboard focus hands control to the reader for good — touch screens have
  // no hover, so readers there previously could not stop it. Reduced-motion
  // users get no auto-advance at all.
  var reduce = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  var timer = null;
  var readerControl = false;
  function start() {
    if (reduce || readerControl || timer) return;
    timer = setInterval(function() { goTo((current + 1) % count); }, 6000);
  }
  function stop() { if (timer) { clearInterval(timer); timer = null; } }
  function takeControl() { readerControl = true; stop(); }
  start();

  dots.addEventListener('click', function(e) {
    var dot = e.target.closest('.carousel-dot');
    if (!dot) return;
    takeControl();
    goTo(parseInt(dot.getAttribute('data-index')));
  });

  var frame = track.parentElement;
  frame.addEventListener('mouseenter', stop);
  frame.addEventListener('mouseleave', start);
  frame.addEventListener('focusin', takeControl);

  // Swipe between testimonials on touch screens.
  var x0 = null, y0 = null;
  frame.addEventListener('touchstart', function(e) {
    takeControl();
    x0 = e.touches[0].clientX; y0 = e.touches[0].clientY;
  }, { passive: true });
  frame.addEventListener('touchend', function(e) {
    if (x0 === null) return;
    var dx = e.changedTouches[0].clientX - x0, dy = e.changedTouches[0].clientY - y0;
    x0 = null;
    if (Math.abs(dx) > 40 && Math.abs(dx) > Math.abs(dy)) goTo((current + (dx < 0 ? 1 : count - 1)) % count);
  }, { passive: true });
})();
</script>
