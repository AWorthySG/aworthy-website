---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import PdfPreviewModal from '../components/PdfPreviewModal.astro';
import GuideForm from '../components/GuideForm.astro';
import '../styles/global.css';

// Order matters: entries with a samplePdf (taller cards with the faux-page tile)
// come first so they fill whole grid rows; the compact request-only cards follow.
const resources = [
  {
    title: "O-Level English Scoring Guide",
    description: "How each part of the paper is marked, what examiners reward, and what separates a C answer from an A answer. With grade benchmarks and sample answers.",
    category: "Exam Prep",
    type: "PDF Guide",
    samplePdf: "/docs/samples/scoring-guide-sample.pdf",
  },
  {
    title: "Comprehension Techniques Checklist",
    description: "A one-page checklist for literal, inferential, evaluative and vocabulary-in-context questions. Tick off each technique as you practise.",
    category: "English",
    type: "Checklist",
    samplePdf: "/docs/samples/comprehension-checklist-sample.pdf",
  },
  {
    title: "Essay Writing Framework",
    description: "The SHARP Method for continuous writing on one printable page: planning, paragraph structure, introductions and conclusions.",
    category: "English",
    type: "Quick Reference",
    samplePdf: "/docs/samples/essay-framework-sample.pdf",
  },
  {
    title: "Common Grammar Mistakes to Avoid",
    description: "The 20 grammar errors that turn up most often in O-Level scripts, each with the correction, a short explanation and exercises.",
    category: "English",
    type: "Practice Sheet",
    samplePdf: "/docs/samples/grammar-mistakes-sample.pdf",
  },
  {
    title: "Situational Writing Format Guide",
    description: "How to set out a formal letter, email, report and speech, and which register each needs. With an annotated model answer for each.",
    category: "Exam Prep",
    type: "PDF Guide",
    samplePdf: "/docs/samples/situational-writing-sample.pdf",
  },
  {
    title: "50 High-Impact O-Level Vocabulary Words",
    description: "Grouped by theme, with definitions, model sentences and synonyms. Print it and learn five words a day for ten days.",
    category: "English",
    type: "PDF Guide",
    samplePdf: "/docs/samples/vocabulary-50-sample.pdf",
  },
  {
    title: "Oral Preparation Checklist",
    description: "Structure, timing, topic vocabulary and a self-assessment rubric for the video stimulus response. Use it to practise on your own before the oral exam.",
    category: "Exam Prep",
    type: "Checklist",
  },
  {
    title: "Summary Writing: The 80-Word Challenge",
    description: "How to pick out the main ideas, paraphrase them and stay within the word limit, step by step. With three timed practice passages and answer keys.",
    category: "English",
    type: "Practice Sheet",
  },
  {
    title: "Common Inference Question Patterns",
    description: "The five inference question types that come up most, and how to answer each with the SHARP Method. With worked examples from past papers.",
    category: "English",
    type: "Quick Reference",
  },
  {
    title: "Editing Paper Strategies",
    description: "How to find and fix the grammar errors in Paper 1 editing, one category at a time. With practice exercises and an answer key.",
    category: "Exam Prep",
    type: "Practice Sheet",
  },
  {
    title: "Essay Hooks That Work",
    description: "Ways to open narrative, expository and argumentative essays. Ten openers, each with a before-and-after example.",
    category: "Study Tips",
    type: "Quick Reference",
  },
  {
    title: "Mock Paper Self-Assessment Guide",
    description: "How to check your own mock papers with the SHARP checklist. With a printable marking rubric and a reflection template.",
    category: "Exam Prep",
    type: "PDF Guide",
  },
  {
    title: "GP Model Essays Collection",
    description: "Five model GP essays on Science & Technology, Media, Environment, Globalisation, and Arts & Culture. Each follows the SHARP Method structure, with examiner-style notes and band descriptors.",
    category: "General Paper",
    type: "Model Essays",
  },
  {
    title: "H2 Economics Case Study Toolkit",
    description: "A step-by-step way to answer A-Level Economics case study questions, from reading the extracts to using the data. With three fully worked case studies and model answers.",
    category: "Economics",
    type: "Toolkit",
  },
  {
    title: "2026 A-Level Exam Predictions",
    description: "The topics we think are likely for H1 GP and H2 Economics, based on the syllabus, past-year patterns and current affairs. Updated for the November 2026 sitting.",
    category: "Exam Prep",
    type: "Predictions",
  },
  {
    title: "O-Level English Model Compositions",
    description: "Four model compositions: narrative, expository, argumentative and descriptive. Each comes with planning notes, the first draft beside the final one, and examiner-style feedback.",
    category: "English",
    type: "Model Essays",
  },
  {
    title: "Economics Diagram Master Sheet",
    description: "The H2 Economics diagrams you need for the A-Level exam on one printable sheet: demand-supply, market failure, AD-AS, exchange rates and the Phillips curve, all correctly labelled.",
    category: "Economics",
    type: "Quick Reference",
  },
];

const categories = ["All", "English", "General Paper", "Economics", "Exam Prep", "Study Tips"];
---

<BaseLayout title="Free Study Resources | English, Maths, GP & Econs | A-Worthy" description="Free study resources from A-Worthy: O-Level English and Maths guides, GP essay frameworks and H2 Economics diagram sheets, written for the SEAB syllabuses.">
  <Header slot="header" />

  <!-- ===== HERO ===== -->
  <section class="r-hero">
    <div class="r-container">
      <span class="eyebrow">Resources</span>
      <h1 class="r-hero-heading"><span class="accent-word">Free</span> O-Level English tuition resources and study guides</h1>
      <p class="r-hero-sub">Study guides, checklists and exam frameworks written by A-Worthy, mostly for O-Level English. Samples open straight away; full versions come by email.</p>
    </div>
    <img src="/images/mascots/mascot-reading.webp" alt="" width="477" height="640" class="sticker sticker--hero" fetchpriority="high" />
  </section>

  <!-- ===== FREE SUBJECT GUIDES ===== -->
  <section class="r-guide" id="free-guide">
    <div class="r-container">
      <GuideForm id="guide-resources" />
    </div>
  </section>

  <!-- ===== SEARCH + FILTERS ===== -->
  <section class="r-filters" data-animate="reveal-up">
    <div class="r-container">
      <div class="r-search-wrap">
        <label for="resource-search" class="sr-only">Search resources</label>
        <input
          type="text"
          id="resource-search"
          class="r-search"
          placeholder="Search guides…"
          autocomplete="off"
        />
      </div>

      <div class="r-filter-row" role="group" aria-label="Filter by category">
        {categories.map((cat) => (
          <button
            class:list={["r-filter-btn", "chip", { active: cat === "All" }]}
            data-category={cat}
          >
            {cat}
          </button>
        ))}
      </div>
    </div>
  </section>

  <!-- ===== RESOURCE CARDS ===== -->
  <section class="r-cards-section">
    <div class="r-container">

      <!-- Reading Room catalogue header (claudedesign F.3) -->
      <header class="r-catalogue-head" data-animate="reveal-up">
        <span class="eyebrow">All guides</span>
        <p class="r-catalogue-copy">
          <span class="r-count">{resources.length}</span>
          guides. Open a sample, or ask for the full&nbsp;copy.
        </p>
      </header>

      <div class="r-grid" id="resources-grid">
        {resources.map((resource, i) => (
          <article
            class:list={["r-card", { "r-card--has-sample": !!resource.samplePdf }]}
            data-category={resource.category}
            data-title={resource.title.toLowerCase()}
            data-description={resource.description.toLowerCase()}
            data-animate="reveal-up"
            data-animate-delay={String((i % 3) + 1)}
          >
            <div class="r-card-top">
              <span class="eyebrow">{resource.category}</span>
              <span class:list={["r-type-badge", "chip", { "chip--green": !!resource.samplePdf }]}>
                {resource.samplePdf ? "Sample available" : resource.type}
              </span>
            </div>
            <h3 class="r-card-title">{resource.title}</h3>
            <p class="r-card-desc">{resource.description}</p>

            {resource.samplePdf ? (
              <Fragment>
                <div class="r-card-inline-preview">
                  <div class="r-card-preview-thumb" aria-hidden="true">
                    <div class="r-prev-page">
                      <div class="r-prev-eyebrow">A-Worthy &middot; {resource.category}</div>
                      <div class="r-prev-title">{resource.title}</div>
                      <div class="r-prev-rule"></div>
                      <div class="r-prev-line" style="width: 88%"></div>
                      <div class="r-prev-line" style="width: 74%"></div>
                      <div class="r-prev-line" style="width: 92%"></div>
                      <div class="r-prev-line" style="width: 68%"></div>
                      <div class="r-prev-line" style="width: 84%"></div>
                      <span class="r-prev-page-num">2 / 8</span>
                    </div>
                    <div class="r-card-preview-blur-overlay">
                      <button
                        type="button"
                        class="r-card-preview-unlock chip chip--red"
                        data-preview-pdf={resource.samplePdf}
                        data-preview-title={resource.title}
                        tabindex="-1"
                      >
                        <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
                        Preview
                      </button>
                    </div>
                  </div>
                </div>
                <div class="r-card-actions">
                  <button
                    class="r-card-preview-btn btn btn-accent btn--sm"
                    data-preview-pdf={resource.samplePdf}
                    data-preview-title={resource.title}
                    type="button"
                  >
                    <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
                    Preview sample
                  </button>
                  <a
                    class="r-card-download-btn btn btn--sm plausible-event-name=Resource+Download"
                    href={resource.samplePdf}
                    data-resource-title={resource.title}
                    data-resource-mode="download"
                    download
                  >
                    <svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/></svg>
                    Download
                  </a>
                </div>
              </Fragment>
            ) : (
              <a
                href="#"
                class="r-card-link btn btn--sm"
                data-resource-title={resource.title}
                data-resource-mode="request"
              >
                Request a free copy <span class="r-arrow">&rarr;</span>
              </a>
            )}
          </article>
        ))}
      </div>

      <!-- No Results -->
      <div class="r-no-results" id="no-results" style="display: none;">
        <p class="r-no-results-heading">No guides found</p>
        <p class="r-no-results-sub">Try a different word, or choose All.</p>
      </div>
    </div>
  </section>

  <!-- ===== NEWSLETTER ===== -->
  <section class="r-newsletter-section">
    <div class="r-container">
      <div class="r-newsletter-card" data-animate="reveal-up">
        <span class="eyebrow">New guides</span>
        <h2 class="r-newsletter-heading">Get new resources in your inbox</h2>
        <p class="r-newsletter-sub">New worksheets, study tips and exam guides, emailed as we write them.</p>
        <form class="r-newsletter-form" id="newsletter-form" action="https://formspree.io/f/xreoozkk" method="POST" data-lead-form>
          <div class="r-newsletter-row">
            <label for="newsletter-email" class="sr-only">Email address</label>
            <input
              type="email"
              id="newsletter-email"
              name="email"
              class="r-newsletter-input"
              autocomplete="email"
              placeholder="you@email.com"
              required
            />
            <button type="submit" class="r-btn-subscribe btn btn-accent" id="newsletter-submit">Subscribe</button>
          </div>
          <p class="r-newsletter-note">Unsubscribe at any time.</p>
          <p class="r-newsletter-success" id="newsletter-success" hidden>Thanks, you&rsquo;re subscribed.</p>
        </form>
      </div>
    </div>
  </section>

  <!-- ===== CTA ===== -->
  <section class="r-cta" data-track="final-cta">
    <div class="r-container">
      <span class="eyebrow">Next step</span>
      <h2 class="r-cta-heading" data-animate="reveal-up">Find out where your child is losing marks</h2>
      <p class="r-cta-sub">The guides explain the methods. To see which ones your child needs, get the free assessment: Jeremy goes through their latest test or exam paper and sends you a written report on the gaps.</p>
      <a href="/contact/" class="r-btn-cta btn btn-accent">Get a Free Report</a>
    </div>
  </section>

  <PdfPreviewModal />
  <Footer slot="footer" />
</BaseLayout>

<script>
  const searchInput = document.getElementById('resource-search') as HTMLInputElement;
  const filterButtons = document.querySelectorAll('.r-filter-btn');
  const resourceCards = document.querySelectorAll('.r-card') as NodeListOf<HTMLElement>;
  const noResults = document.getElementById('no-results') as HTMLElement;

  let activeCategory = 'All';

  function filterResources() {
    const searchTerm = searchInput.value.toLowerCase().trim();
    let visibleCount = 0;

    resourceCards.forEach((card) => {
      const title = card.getAttribute('data-title') || '';
      const description = card.getAttribute('data-description') || '';
      const category = card.getAttribute('data-category') || '';

      const matchesCategory = activeCategory === 'All' || category === activeCategory;
      const matchesSearch = !searchTerm || title.includes(searchTerm) || description.includes(searchTerm);

      if (matchesCategory && matchesSearch) {
        card.style.display = '';
        visibleCount++;
      } else {
        card.style.display = 'none';
      }
    });

    noResults.style.display = visibleCount === 0 ? 'flex' : 'none';
  }

  searchInput.addEventListener('input', filterResources);

  filterButtons.forEach((btn) => {
    btn.addEventListener('click', () => {
      filterButtons.forEach((b) => b.classList.remove('active'));
      btn.classList.add('active');
      activeCategory = btn.getAttribute('data-category') || 'All';
      filterResources();
    });
  });

  const newsletterForm = document.getElementById('newsletter-form') as HTMLFormElement;
  const newsletterSuccess = document.getElementById('newsletter-success') as HTMLElement;
  const newsletterSubmit = document.getElementById('newsletter-submit') as HTMLButtonElement;

  newsletterForm?.addEventListener('submit', async (e) => {
    e.preventDefault();
    const emailInput = document.getElementById('newsletter-email') as HTMLInputElement;
    if (!emailInput.value) return;

    newsletterSubmit.disabled = true;
    newsletterSubmit.textContent = 'Subscribing…';

    try {
      const formData = new FormData(newsletterForm);
      const response = await fetch(newsletterForm.action, {
        method: 'POST',
        body: formData,
        headers: { 'Accept': 'application/json' },
      });

      if (response.ok) {
        emailInput.value = '';
        newsletterSuccess.hidden = false;
      } else {
        throw new Error('Failed');
      }
    } catch {
      newsletterSuccess.hidden = false;
      newsletterSuccess.textContent = 'Sorry, that didn’t go through. Please try again.';
    } finally {
      newsletterSubmit.disabled = false;
      newsletterSubmit.textContent = 'Subscribe';
    }
  });
</script>

<script is:inline>
(function(){
  var FORMSPREE_ID = 'xreoozkk';
  var FORMSPREE_URL = 'https://formspree.io/f/' + FORMSPREE_ID;

  function getStoredLead() {
    try {
      var raw = localStorage.getItem('resource-lead');
      return raw ? JSON.parse(raw) : null;
    } catch (e) { return null; }
  }

  function storeLead(email, phone, name) {
    try {
      localStorage.setItem('resource-lead', JSON.stringify({ email: email, phone: phone, name: name || '' }));
    } catch (e) {}
  }

  function submitLead(payload) {
    return fetch(FORMSPREE_URL, {
      method: 'POST',
      headers: { 'Accept': 'application/json', 'Content-Type': 'application/json' },
      body: JSON.stringify(payload)
    });
  }

  function showToast(message) {
    var toast = document.createElement('div');
    toast.className = 'resource-toast';
    toast.textContent = message;
    document.body.appendChild(toast);
    setTimeout(function() { toast.classList.add('resource-toast--show'); }, 10);
    setTimeout(function() {
      toast.classList.remove('resource-toast--show');
      setTimeout(function() { toast.remove(); }, 300);
    }, 4500);
  }

  function deliverResource(mode, url, title) {
    if (mode === 'download' && url) {
      window.open(url, '_blank');
      showToast('The sample is open in a new tab. We’ll email you the full version of "' + title + '" within 24 hours.');
    } else {
      showToast('Thanks. We’ll email you the full version of "' + title + '" within 24 hours.');
    }
  }

  function openGate(mode, url, title) {
    var overlay = document.createElement('div');
    overlay.className = 'resource-gate-overlay';
    var ctaLabel = mode === 'download' ? 'Send me the guide' : 'Email me the full version';
    var heading = mode === 'download'
      ? 'Open the sample now and get the full version by email'
      : 'Get the full version by email';
    var subline = mode === 'download'
      ? 'Leave your details and the sample opens straight away. We’ll also email you the full version of "' + title + '".'
      : 'Leave your details and we’ll email you "' + title + '" within 24 hours.';

    var waMessage = encodeURIComponent('Hi A-Worthy, could you send me the full version of "' + title + '"?');
    var waUrl = 'https://wa.me/6589913181?text=' + waMessage;

    overlay.innerHTML =
      '<div class="resource-gate" role="dialog" aria-modal="true" aria-labelledby="resource-gate-title">' +
        '<button class="resource-gate-close" aria-label="Close">&times;</button>' +
        '<span class="resource-gate-eyebrow">Free guide</span>' +
        '<h4 id="resource-gate-title">' + heading + '</h4>' +
        '<p>' + subline + '</p>' +
        '<form class="resource-gate-form" novalidate>' +
          '<label class="resource-gate-label">' +
            '<span>Email <em>*</em></span>' +
            '<input type="email" name="email" required autocomplete="email" placeholder="you@email.com" />' +
          '</label>' +
          '<label class="resource-gate-label">' +
            '<span>Phone (Singapore) <em>*</em></span>' +
            '<input type="tel" name="phone" required autocomplete="tel" inputmode="tel" placeholder="+65 9123 4567" />' +
          '</label>' +
          '<label class="resource-gate-label">' +
            '<span>Name <em class="resource-gate-optional">(optional)</em></span>' +
            '<input type="text" name="name" autocomplete="name" placeholder="Parent or student name" />' +
          '</label>' +
          '<button type="submit" class="resource-gate-submit">' + ctaLabel + '</button>' +
          '<p class="resource-gate-error" role="alert" hidden></p>' +
          '<p class="resource-gate-note">No spam, and you can unsubscribe at any time.</p>' +
        '</form>' +
        '<div class="resource-gate-divider"><span>or</span></div>' +
        '<a class="resource-gate-whatsapp" href="' + waUrl + '" target="_blank" rel="noopener noreferrer">' +
          '<svg aria-hidden="true" width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M.057 24l1.687-6.163c-1.041-1.804-1.588-3.849-1.587-5.946.003-6.556 5.338-11.891 11.893-11.891 3.181.001 6.167 1.24 8.413 3.488 2.245 2.248 3.481 5.236 3.48 8.414-.003 6.557-5.338 11.892-11.893 11.892-1.99-.001-3.951-.5-5.688-1.448l-6.305 1.654zm6.597-3.807c1.676.995 3.276 1.591 5.392 1.592 5.448 0 9.886-4.434 9.889-9.885.002-5.462-4.415-9.89-9.881-9.892-5.452 0-9.887 4.434-9.889 9.884-.001 2.225.651 3.891 1.746 5.634l-.999 3.648 3.742-.981zm11.387-5.464c-.074-.124-.272-.198-.57-.347-.297-.149-1.758-.868-2.031-.967-.272-.099-.47-.149-.669.149-.198.297-.768.967-.941 1.165-.173.198-.347.223-.644.074-.297-.149-1.255-.462-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.297-.347.446-.521.151-.172.2-.296.3-.495.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51l-.57-.01c-.198 0-.52.074-.792.372s-1.04 1.016-1.04 2.479 1.065 2.876 1.213 3.074c.149.198 2.095 3.2 5.076 4.487.711.307 1.265.489 1.697.626.713.226 1.362.194 1.875.118.572-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413z"/></svg>' +
          'Ask for it on WhatsApp' +
        '</a>' +
      '</div>';

    document.body.appendChild(overlay);
    document.body.style.overflow = 'hidden';

    function close() {
      overlay.remove();
      document.body.style.overflow = '';
    }

    overlay.querySelector('.resource-gate-close').addEventListener('click', close);
    overlay.addEventListener('click', function(ev) {
      if (ev.target === overlay) close();
    });
    document.addEventListener('keydown', function escHandler(ev) {
      if (ev.key === 'Escape') { close(); document.removeEventListener('keydown', escHandler); }
    });

    setTimeout(function() {
      var firstInput = overlay.querySelector('input[name="email"]');
      if (firstInput) firstInput.focus();
    }, 50);

    var form = overlay.querySelector('form');
    var submitBtn = overlay.querySelector('.resource-gate-submit');
    var errorEl = overlay.querySelector('.resource-gate-error');

    form.addEventListener('submit', function(ev) {
      ev.preventDefault();
      var email = form.email.value.trim();
      var phone = form.phone.value.trim();
      var name = form.name.value.trim();

      var phoneDigits = phone.replace(/\D/g, '');
      if (!email || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
        errorEl.textContent = 'That email address doesn’t look complete. Please check it.';
        errorEl.hidden = false;
        return;
      }
      if (phoneDigits.length < 8) {
        errorEl.textContent = 'Please enter a phone number with at least 8 digits.';
        errorEl.hidden = false;
        return;
      }
      errorEl.hidden = true;

      submitBtn.disabled = true;
      submitBtn.textContent = 'Sending…';

      var payload = {
        email: email,
        phone: phone,
        name: name,
        resource: title,
        mode: mode,
        _subject: 'Resource Download Request: ' + title
      };
      if (typeof window.aworthyAttribution === 'function') {
        try {
          var attr = window.aworthyAttribution() || {};
          Object.keys(attr).forEach(function(k) { if (!(k in payload)) payload[k] = attr[k]; });
        } catch (err) {}
      }

      submitLead(payload).then(function(res) {
        if (!res.ok) throw new Error('submit-failed');
        if (typeof window.plausible === 'function') window.plausible('Lead: Resource Gate');
        storeLead(email, phone, name);
        close();
        deliverResource(mode, url, title);
      }).catch(function() {
        // Still store locally and deliver so the user isn't blocked by a Formspree outage.
        storeLead(email, phone, name);
        close();
        deliverResource(mode, url, title);
      });
    });
  }

  function handleClick(e) {
    var el = e.currentTarget;
    var mode = el.getAttribute('data-resource-mode');
    var title = el.getAttribute('data-resource-title') || 'A-Worthy resource';
    var url = el.getAttribute('href');
    var stored = getStoredLead();

    if (stored && stored.email && stored.phone) {
      // Returning visitor: silently log the request and deliver.
      submitLead({
        email: stored.email,
        phone: stored.phone,
        name: stored.name || '',
        resource: title,
        mode: mode,
        repeat: true,
        _subject: 'Resource Re-download: ' + title
      }).catch(function() {});

      if (mode === 'request') {
        e.preventDefault();
        deliverResource(mode, url, title);
      }
      // For mode === 'download', let the native <a download> open the file.
      return;
    }

    e.preventDefault();
    openGate(mode, url, title);
  }

  document.querySelectorAll('[data-resource-mode]').forEach(function(el) {
    el.addEventListener('click', handleClick);
  });
})();
</script>

<style is:global>
  /* Email gate + toast are created by script at runtime, so this block is
     is:global — a scoped block would never match them. Sticker-book look:
     white ink card, muted inputs, one red submit. */
  /* The overlay scrolls and the card centres with auto margins (not just
     align-items: center): on a landscape phone the ~640px card is taller than
     the screen, and a centred flex item overflows off the TOP where nothing can
     scroll to it — the heading and close button were unreachable. Auto margins
     collapse to 0 when the card does not fit, so it starts at the top instead. */
  .resource-gate-overlay {
    position: fixed;
    inset: 0;
    background: rgba(28, 27, 25, 0.55);
    z-index: var(--z-modal);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1rem;
    overflow-y: auto;
    overscroll-behavior: contain;
  }
  .resource-gate {
    background: var(--bg-card);
    border: 2px solid var(--color-ink);
    border-radius: var(--radius-md);
    box-shadow: 0 4px 0 var(--ink-shadow);
    padding: 2rem 1.75rem 1.5rem;
    max-width: 420px;
    width: 100%;
    margin: auto;
    position: relative;
  }
  .resource-gate-close {
    position: absolute;
    top: 0.5rem;
    right: 0.75rem;
    background: none;
    border: none;
    font-size: 1.5rem;
    line-height: 1;
    color: var(--text-secondary);
    cursor: pointer;
    padding: 0.25rem 0.5rem;
    min-width: 40px;
    min-height: 40px;
  }
  .resource-gate-close:hover {
    color: var(--text-primary);
  }
  .resource-gate-eyebrow {
    display: inline-block;
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--text-secondary);
    margin-bottom: 0.5rem;
  }
  .resource-gate h4 {
    font-family: var(--font-heading);
    font-size: 1.15rem;
    font-weight: 800;
    color: var(--text-primary);
    margin: 0 0 0.5rem;
    line-height: 1.35;
  }
  .resource-gate p {
    font-size: 0.85rem;
    color: var(--text-secondary);
    margin: 0 0 1.25rem;
    line-height: 1.55;
  }
  .resource-gate-form {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }
  .resource-gate-label {
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
    margin-bottom: 0;
  }
  .resource-gate-label > span {
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--text-secondary);
  }
  .resource-gate-label em {
    font-style: normal;
    color: var(--color-accent-text);
    margin-left: 0.15rem;
  }
  .resource-gate-label em.resource-gate-optional {
    color: var(--text-tertiary);
    font-weight: 400;
    text-transform: none;
    letter-spacing: 0;
  }
  .resource-gate-form input {
    padding: 0.7rem 0.85rem;
    font-size: max(1rem, 16px);
  }
  /* Red sticker pill — mirrors the global .btn-accent */
  .resource-gate-submit {
    margin-top: 0.25rem;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 44px;
    padding: 0.8rem 1.5rem;
    font-family: var(--font-body);
    font-size: 0.95rem;
    font-weight: 800;
    color: #fff;
    background: var(--color-accent);
    border: 2px solid var(--color-ink);
    border-radius: var(--radius-pill);
    box-shadow: 0 4px 0 var(--color-accent-press);
    cursor: pointer;
    transition: background-color 0.12s ease, transform 0.1s ease, box-shadow 0.1s ease;
  }
  .resource-gate-submit:hover:not(:disabled) {
    background: var(--color-accent-hover);
  }
  .resource-gate-submit:active:not(:disabled) {
    transform: translateY(3px);
    box-shadow: 0 0 0 var(--color-accent-press);
  }
  .resource-gate-submit:disabled {
    opacity: 0.7;
    cursor: not-allowed;
  }
  .resource-gate-error {
    color: var(--color-accent-text);
    font-size: 0.8rem;
    font-weight: 700;
    margin: 0;
  }
  .resource-gate-note {
    font-size: 0.72rem;
    color: var(--text-secondary);
    margin: 0;
    text-align: center;
    line-height: 1.5;
  }

  .resource-gate-divider {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin: 1rem 0 0.85rem;
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--text-secondary);
  }
  .resource-gate-divider::before,
  .resource-gate-divider::after {
    content: '';
    flex: 1;
    border-top: 2px dashed var(--ink-faint);
  }

  .resource-gate-whatsapp {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    min-height: 44px;
    padding: 0.7rem 1rem;
    background: var(--color-whatsapp);
    color: #0E2B19;
    border: 2px solid var(--color-ink);
    border-radius: var(--radius-pill);
    box-shadow: 0 4px 0 var(--ink-shadow);
    font-family: var(--font-body);
    font-size: 0.9rem;
    font-weight: 800;
    text-decoration: none;
    transition: background-color 0.12s ease, transform 0.1s ease, box-shadow 0.1s ease;
  }
  .resource-gate-whatsapp:hover {
    background: #4BE07F;
    color: #0E2B19;
  }
  .resource-gate-whatsapp:active {
    transform: translateY(3px);
    box-shadow: 0 0 0 var(--ink-shadow);
  }
  .resource-gate-whatsapp svg {
    flex-shrink: 0;
  }

  /* ===== TOAST — ink band, sits above the sticky bar ===== */
  .resource-toast {
    position: fixed;
    bottom: calc(max(1.5rem, env(safe-area-inset-bottom)) + var(--sticky-bar-h, 0px));
    left: 50%;
    transform: translate(-50%, 1rem);
    z-index: var(--z-toast);
    background: var(--color-ink);
    color: #fff;
    border: 2px solid var(--color-ink);
    font-family: var(--font-body);
    font-size: 0.85rem;
    font-weight: 600;
    line-height: 1.5;
    padding: 0.85rem 1.25rem;
    border-radius: var(--radius-sm);
    max-width: min(420px, calc(100% - 2rem));
    box-shadow: 0 4px 0 var(--ink-shadow);
    opacity: 0;
    transition: opacity 0.25s ease, transform 0.25s ease;
    pointer-events: none;
  }
  [data-theme="dark"] .resource-toast {
    background: var(--bg-card);
    color: var(--text-primary);
  }
  .resource-toast--show {
    opacity: 1;
    transform: translate(-50%, 0);
  }

  @media (max-width: 480px) {
    .resource-gate {
      padding: 1.75rem 1.25rem 1.25rem;
    }
  }
</style>

<style>
  /* Uses the sticker-book tokens + vocabulary from global.css (ink cards,
     chips, red action colour, grey micro-labels). */

  /* ===== SHARED ===== */
  .r-container {
    max-width: 1120px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  /* ===== HERO ===== */
  .r-hero {
    padding: 1.75rem 0;
    text-align: center;
  }

  .r-hero-heading {
    font-family: var(--font-heading);
    font-size: clamp(2.2rem, 5vw, 3.4rem);
    font-weight: 900;
    color: var(--text-primary);
    letter-spacing: -0.03em;
    line-height: 1.1;
    margin: 0 0 1.25rem;
  }

  .r-hero-sub {
    font-family: var(--font-body);
    font-size: 1.1rem;
    color: var(--text-secondary);
    line-height: 1.7;
    max-width: 540px;
    margin: 0 auto;
  }

  /* ===== FREE SUBJECT GUIDES ===== */
  .r-guide {
    padding: 0 0 2rem;
    scroll-margin-top: var(--nav-offset);
  }

  /* ===== SEARCH + FILTERS ===== */
  .r-filters {
    padding: 0 0 3rem;
  }

  .r-search-wrap {
    max-width: 480px;
    margin: 0 auto 2rem;
  }

  /* .r-search takes the global input look (muted fill, 2px ink, red focus ring) */

  .r-filter-row {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.4rem;
  }

  /* Filter buttons are .chip pills; the active one takes the red chip tint */
  .r-filter-btn {
    min-height: 40px;
    padding: 0.45rem 0.95rem;
    cursor: pointer;
    transition: color var(--transition-fast), background-color var(--transition-fast), border-color var(--transition-fast);
  }

  @media (hover: hover) {
    .r-filter-btn:hover {
      color: var(--text-primary);
      background: var(--bg-hover);
      border-color: var(--color-ink);
    }
  }

  .r-filter-btn.active {
    color: var(--color-accent-text);
    background: var(--color-danger-bg);
    border-color: var(--color-accent);
  }

  /* ===== RESOURCE CARDS ===== */
  .r-cards-section {
    padding: 0 0 5rem;
  }

  /* Reading Room catalogue header */
  .r-catalogue-head {
    max-width: 760px;
    margin: 0 auto 3rem;
    text-align: center;
    padding: 2.5rem 0 0;
  }
  .r-catalogue-head .eyebrow {
    margin-bottom: 1.25rem;
  }
  .r-catalogue-copy {
    font-family: var(--font-heading);
    font-weight: 700;
    font-size: clamp(1.2rem, 2.1vw, 1.45rem);
    line-height: 1.5;
    color: var(--text-secondary);
    text-wrap: pretty;
    margin: 0;
  }
  .r-count {
    font-family: var(--font-heading);
    font-weight: 900;
    font-size: clamp(2.4rem, 4vw, 3.2rem);
    letter-spacing: -0.03em;
    color: var(--color-accent-text);
    margin-right: 0.25rem;
    font-feature-settings: "tnum" 1, "lnum" 1;
    font-variant-numeric: tabular-nums lining-nums;
    vertical-align: middle;
    line-height: 1;
  }
  .r-catalogue-head::after {
    content: '';
    display: block;
    width: 88px;
    height: 14px;
    margin: 1.25rem auto 0;
    background: var(--swash) center / contain no-repeat;
  }

  .r-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1.25rem;
    /* Natural heights: a compact card must never stretch to a sample card's row
       (it would show a blank white block between blurb and link). Sample cards
       opt back in below so their own rows are not ragged. */
    align-items: start;
  }

  /* Sample cards are all the same kind and sit together at the top of the grid,
     so they stretch to a common row height (the compact cards below do not). */
  .r-card--has-sample {
    align-self: stretch;
  }

  /* Ink card — white sticker on the paper, 3px hard rest shadow, lifts on hover */
  .r-card {
    display: flex;
    flex-direction: column;
    background: var(--bg-card);
    border: 2px solid var(--color-ink);
    border-radius: var(--radius-md);
    box-shadow: 0 3px 0 var(--ink-shadow);
    padding: 2rem 1.75rem;
    transition: transform 0.22s cubic-bezier(0.22, 1, 0.36, 1), box-shadow 0.22s ease;
  }

  @media (hover: hover) {
    .r-card:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 0 var(--ink-shadow), 0 12px 24px rgba(70, 50, 20, 0.10);
    }
  }

  .r-card-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
  }

  .r-card-top .eyebrow {
    margin-bottom: 0;
  }

  /* .r-type-badge is a .chip (chip--green when a sample is available) */

  .r-card-title {
    font-family: var(--font-heading);
    font-size: 1.1rem;
    font-weight: 800;
    color: var(--text-primary);
    line-height: 1.4;
    margin: 0 0 0.75rem;
    letter-spacing: -0.01em;
  }

  .r-card-desc {
    font-family: var(--font-body);
    font-size: 0.9375rem;
    color: var(--text-secondary);
    line-height: 1.65;
    flex-grow: 1;
    margin: 0 0 1.25rem;
  }

  /* "Request Free Copy" is a .btn.btn--sm sticker pill — the same treatment as
     "Download" on the sample cards; only its placement is page-specific */
  .r-card-link {
    align-self: flex-start;
    margin-top: auto;
    white-space: nowrap;
  }

  /* ===== CARD ACTIONS (preview = red pill, download = white pill) ===== */
  .r-card-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: auto;
  }

  .r-card-preview-btn,
  .r-card-download-btn {
    flex: 1;
    white-space: nowrap;
  }

  .r-arrow {
    font-size: 1rem;
    transition: transform 0.2s ease;
  }

  /* ===== INLINE PREVIEW — faux paper page on a muted, ink-outlined tile ===== */
  .r-card-inline-preview {
    margin-bottom: 1rem;
    overflow: visible;
    border: 0;
  }

  .r-card-preview-thumb {
    position: relative;
  }

  .r-prev-page {
    position: relative;
    /* Cropped top of a page, not a full A4 sheet — the tile only carries ~6 lines */
    aspect-ratio: 1 / 1.05;
    background: var(--bg-muted);
    border: 1.5px solid var(--color-ink);
    border-radius: var(--radius-sm);
    padding: 1.1rem 1.2rem;
    overflow: hidden;
  }
  .r-prev-eyebrow {
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--text-secondary);
    margin-bottom: 0.4rem;
  }
  .r-prev-title {
    font-family: var(--font-heading);
    font-weight: 800;
    font-size: 1rem;
    line-height: 1.2;
    color: var(--text-primary);
    margin-bottom: 0.45rem;
    text-wrap: balance;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .r-prev-rule {
    width: 2.6rem;
    height: 8px;
    background: var(--swash) left center / contain no-repeat;
    margin: 0.4rem 0 0.7rem;
  }
  .r-prev-line {
    height: 5px;
    background: var(--ink-faint);
    opacity: 0.55;
    border-radius: 2px;
    margin-bottom: 0.45rem;
  }
  .r-prev-page-num {
    position: absolute;
    right: 0.9rem;
    bottom: 0.7rem;
    font-size: 0.72rem;
    font-weight: 700;
    color: var(--text-secondary);
    letter-spacing: 0.1em;
  }

  /* Fade the faux page into the tile so the "Preview" chip sits on a calm edge */
  .r-card-preview-blur-overlay {
    position: absolute;
    inset: 1.5px;
    border-radius: calc(var(--radius-sm) - 1.5px);
    background: linear-gradient(180deg, transparent 35%, var(--bg-muted) 92%);
    display: flex;
    align-items: flex-end;
    justify-content: center;
    padding-bottom: 0.75rem;
    pointer-events: none;
  }

  /* .r-card-preview-unlock is a .chip.chip--red rendered as a real <button> that
     opens the same PDF modal as "Preview Sample" (the modal delegates on
     [data-preview-pdf]). It sits inside the aria-hidden thumb with tabindex=-1, so
     it is a pointer-only shortcut — keyboard/AT users get the button below.
     ::after enlarges the tap box to ≥40px without changing the chip's size. */
  .r-card-preview-unlock {
    position: relative;
    cursor: pointer;
    box-shadow: 0 2px 0 var(--ink-shadow);
    pointer-events: auto;
    transition: transform 0.1s ease, box-shadow 0.1s ease;
  }
  .r-card-preview-unlock::after {
    content: '';
    position: absolute;
    inset: -8px -4px;
  }
  .r-card-preview-unlock:active {
    transform: translateY(2px);
    box-shadow: 0 0 0 var(--ink-shadow);
  }

  /* ===== NO RESULTS ===== */
  .r-no-results {
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 1.75rem 0;
  }

  .r-no-results-heading {
    font-family: var(--font-heading);
    font-size: 1.15rem;
    font-weight: 800;
    color: var(--text-primary);
    margin: 0 0 0.5rem;
  }

  .r-no-results-sub {
    font-family: var(--font-body);
    font-size: 0.9rem;
    color: var(--text-secondary);
    margin: 0;
  }

  /* ===== NEWSLETTER — ink card, red submit ===== */
  .r-newsletter-section {
    padding: 0 0 5rem;
  }

  .r-newsletter-card {
    background: var(--bg-card);
    border: 2px solid var(--color-ink);
    border-radius: var(--radius-md);
    box-shadow: 0 3px 0 var(--ink-shadow);
    padding: 3rem;
    text-align: center;
    max-width: 600px;
    margin: 0 auto;
  }

  .r-newsletter-heading {
    font-family: var(--font-heading);
    font-size: 1.5rem;
    font-weight: 800;
    color: var(--text-primary);
    letter-spacing: -0.02em;
    margin: 0 0 0.75rem;
  }

  .r-newsletter-sub {
    font-family: var(--font-body);
    font-size: 0.9rem;
    color: var(--text-secondary);
    line-height: 1.6;
    margin: 0 0 1.75rem;
  }

  .r-newsletter-form {
    width: 100%;
  }

  .r-newsletter-row {
    display: flex;
    gap: 0.5rem;
  }

  /* .r-newsletter-input takes the global input look */
  .r-newsletter-input {
    flex: 1;
  }

  /* .r-btn-subscribe is a .btn.btn-accent */
  .r-btn-subscribe {
    white-space: nowrap;
  }

  .r-newsletter-note {
    font-family: var(--font-body);
    font-size: 0.75rem;
    color: var(--text-secondary);
    margin: 0.75rem 0 0;
  }

  .r-newsletter-success {
    font-family: var(--font-body);
    font-size: 0.9rem;
    color: var(--color-success);
    margin: 0.75rem 0 0;
    font-weight: 700;
  }

  /* ===== CTA ===== */
  .r-cta {
    padding: 2.25rem 0 5rem;
    text-align: center;
  }

  .r-cta-heading {
    font-family: var(--font-heading);
    font-size: clamp(1.5rem, 3vw, 2rem);
    font-weight: 800;
    color: var(--text-primary);
    letter-spacing: -0.02em;
    margin: 0 0 0.75rem;
  }

  .r-cta-sub {
    font-family: var(--font-body);
    font-size: 1rem;
    color: var(--text-secondary);
    margin: 0 0 2rem;
  }

  /* .r-btn-cta is a .btn.btn-accent */

  /* ===== RESPONSIVE ===== */
  @media (max-width: 1024px) {
    .r-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    /* The faux page's height follows its width (1 : 1.05). In the 2-column
       tablet grid the tile is up to ~420px wide (iPad landscape), so it grew
       ~440px tall with only ~190px of lines in it. Fixed height instead of the
       ratio (a max-height
       would shrink the WIDTH through the ratio and leave the fade overlay,
       which fills the card, hanging off the tile). */
    .r-prev-page {
      aspect-ratio: auto;
      height: 15rem;
    }
  }

  @media (max-width: 640px) {
    .r-hero {
      padding: 1.5rem 0 2rem;
    }

    .r-grid {
      grid-template-columns: minmax(0, 1fr);
      gap: 1rem;
    }

    .r-card {
      padding: 1.5rem 1.25rem;
    }

    .r-filter-row {
      gap: 0.35rem;
    }

    .r-filter-btn {
      padding: 0.4rem 0.75rem;
    }

    .r-newsletter-card {
      padding: 2rem 1.5rem;
    }

    .r-newsletter-row {
      flex-direction: column;
    }

    .r-cta {
      padding: 2rem 0 5rem;
    }
  }

  /* html font-size scales up from 1441px (112.5% / 125% / 150%) but these caps
     were in px, so the grid and copy shrank relative to the type: at 2560 the
     cards were ~420px wide for 24px text, eyebrows broke onto two lines and
     "Request Free Copy" wrapped. rem caps keep the laptop proportions
     (70rem = 1120px at 16px, the 1400px the old ≥1921 rule set at 20px). */
  @media (min-width: 1441px) {
    .r-container {
      max-width: 70rem;
    }

    /* 420px at 16px; in px, both gate buttons wrapped to two lines at 2560 */
    .resource-gate {
      max-width: 26.25rem;
    }

    .r-hero-sub {
      max-width: 33.75rem;
    }

    .r-search-wrap {
      max-width: 30rem;
    }

    .r-catalogue-head {
      max-width: 47.5rem;
    }

    .r-newsletter-card {
      max-width: 37.5rem;
    }
  }

  /* global.css re-pads every .btn at ≥1921px, which also overrides .btn--sm;
     keep the card actions compact so they fit their card */
  @media (min-width: 1921px) {
    .r-card-link,
    .r-card-preview-btn,
    .r-card-download-btn {
      padding: 0.5rem 1.15rem;
      font-size: 0.85rem;
    }
  }
</style>
